# =============================================================================
# Beyond Accuracy: Calibration and Uncertainty in Multi-Architecture Ensembles
# for MedMNIST Classification
#
# Python 3.10
#
# Two environments were used in the study and are both covered below:
#   - Base-model TRAINING ran on Kaggle GPU instances under TensorFlow 2.19.0.
#   - EVALUATION and INTERPRETABILITY ran locally on a Windows CPU under
#     TensorFlow 2.21.0 with OpenCV 4.13.0, operating on the saved prediction
#     arrays and weights rather than retraining.
#
# The pins below reproduce the training environment. The prediction-based
# "fast verification" path (loading the .npy files in predictions/) works under
# either TensorFlow build; if reproducing the local evaluation exactly, install
# tensorflow==2.21.0 instead of the line below.
#
# The legacy Keras 2 backend was used throughout (TF_USE_LEGACY_KERAS=1), which
# is why tf-keras is listed as an explicit dependency.
# =============================================================================

# --- Core scientific stack ---------------------------------------------------
numpy==1.26.4
pandas>=2.0
scipy>=1.11

# --- Deep learning -----------------------------------------------------------
tensorflow==2.19.0          # base-model training; use 2.21.0 for local evaluation
tf-keras>=2.19              # legacy Keras 2 backend (TF_USE_LEGACY_KERAS=1)
transformers==4.44.2        # ViT-Base backbone

# --- Metrics, calibration, and the stacking meta-learner support -------------
scikit-learn>=1.3           # precision/recall/F1, ROC-AUC, PR-AUC, log loss, reliability binning

# --- Interpretability (Grad-CAM overlays and ViT attention maps) -------------
opencv-python==4.13.0       # cv2; heatmap overlays in the local evaluation environment

# --- Visualisation -----------------------------------------------------------
matplotlib>=3.7
seaborn>=0.12               # kernel-density entropy distributions

# --- Figure assembly ---------------------------------------------------------
svgutils>=0.3.4             # stacking per-model SVGs into the interpretability collages
Pillow>=10.0

# --- Dataset access ----------------------------------------------------------
medmnist>=3.0               # MedMNIST v2 / MedMNIST+ download (224 x 224 variants)

# --- Notebook environment ----------------------------------------------------
jupyter
notebook
ipykernel

# --- Utilities ---------------------------------------------------------------
tqdm
